apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "capi-k8s-release.fullname" . }}-test-connection"
  labels:
{{ include "capi-k8s-release.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "capi-k8s-release.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
